keybindings:
  # Optional custom overrides:
  # help: "?"
  # quit: "q"
  # switch_focus: "w"
  # refresh: "r"
  # echo_topic: "e"
  # call_service: "c"
  # send_goal: "g"

topics:
  # Example: /turtle1/pose:
  #   plot_fields: ["x", "y"]
  #   plot_type: "line"

services:
  # Example: /spawn:
  #   preset_params:
  #     x: 2.0
  #     y: 2.0

ui:
  auto_refresh_interval_ms: 2000
  mouse_support: true
